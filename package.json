{
  "name": "global-weather",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "export PATH=/opt/render/project/src/node_modules/.bin:$PATH && node_modules/.bin/live-server",
    "build": "npm install"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "live-server": "^1.2.2"
  }
}