{
  "name": "global-weather",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
  "start": "npx live-server",
  "test": "echo \"Error: no test specified\" && exit 1",
  "build": "mkdir dist && cp index.html dist/ && cp -r css dist/ && cp -r js dist/",
  "predeploy": "npm run build"
},
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}