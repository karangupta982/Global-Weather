{
  "name": "global-weather",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "export PATH=/opt/render/project/src/node_modules/.bin:$PATH && live-server",
    "build": "echo 'No build process required for this app'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "live-server": "^1.2.2"
  }
}
